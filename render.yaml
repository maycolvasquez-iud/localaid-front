services:
  - type: static
    name: localaid-frontend
    buildCommand: npm run build
    staticPublishPath: ./out
    env: node
    nodeVersion: 18
    envVars:
      - key: NODE_ENV
        value: production
